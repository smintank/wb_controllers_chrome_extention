{
  "name": "wb-controller-tracker-extension",
  "version": "1.0.0",
  "description": "Chrome extension package for tracking recently opened local Wiren Board controllers and reopening them quickly.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "package:extension": "node scripts/package-extension.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
